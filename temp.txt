// %[флаги][ширина][.точность][размер]тип
// cspdiuxX%

#include "ft_printf.h"
int f_1(char const *format, ...)
{
	va_list	args;
	char *res;
	char es;

	// res = malloc (50);

	va_start(args, format);
	res = va_arg(args, char *);
	if (res == NULL)
		es = 'h';
	write (1, &es, 1);

	va_end(args);
	return (0);
}

int	main(void)
{
	f_1("hello", NULL);
}
